# Tooling Gaps — Debugging Workflow

Living backlog of friction points in the debugging/dev workflow and concrete ideas for solving them. Add a section per gap: the friction, why it matters, and options to fix. Remove a section once it ships (the rationale lives on in git history).

**No open gaps right now.** The three surfaced during the `Competitionetition` bug investigation (2026-05-30) — log-reader content search, `TextPostProcessor` regression tests, and a replacements dry-run — all shipped on 2026-06-01. See `git log` for the details, the `read-logs` skill for `--grep-detail`, `.claude/scripts/test-replacements.py` for the dry-run, and `tests/test_text_post_processor.py` for the regression tests.
